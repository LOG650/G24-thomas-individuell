[project]
name = "mrp-lotstorrelse"
version = "0.1.0"
description = "Code for example: MRP with lot-sizing (lot-for-lot, EOQ, Silver-Meal)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.2",
    "networkx>=3.2",
]
