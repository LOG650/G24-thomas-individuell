[project]
name = "kvantumsrabatt"
version = "0.1.0"
description = "Code for example: EOQ with quantity discounts (all-units vs incremental)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.1",
    "scipy>=1.11",
]
